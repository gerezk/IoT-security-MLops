[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "iot-security-mlops"
version = "3.0.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]